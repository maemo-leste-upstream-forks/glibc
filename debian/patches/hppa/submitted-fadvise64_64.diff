Index: b/sysdeps/unix/sysv/linux/posix_fadvise.c
===================================================================
--- a/sysdeps/unix/sysv/linux/posix_fadvise.c	2008-08-08 19:08:25.000000000 +0200
+++ b/sysdeps/unix/sysv/linux/posix_fadvise.c	2008-08-08 19:08:49.000000000 +0200
@@ -35,6 +35,19 @@
     return INTERNAL_SYSCALL_ERRNO (ret, err);
   return 0;
 #else
+# ifdef __NR_fadvise64_64
+  INTERNAL_SYSCALL_DECL (err);  
+  int ret = INTERNAL_SYSCALL (fadvise64_64, err, 6, fd,
+			      __LONG_LONG_PAIR ((long) (offset >> 31),
+						(long) offset),
+			      __LONG_LONG_PAIR ((long) (len >> 31),
+						(long) len),
+			      advise);
+  if (INTERNAL_SYSCALL_ERROR_P (ret, err))
+    return INTERNAL_SYSCALL_ERRNO (ret, err);
+  return 0;
+# else
   return ENOSYS;
+# endif
 #endif
 }
